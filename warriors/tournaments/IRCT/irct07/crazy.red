;redcode-94x
;name cRaZy
;author Christian Schmidt
;strategy kind of clear
;assert 1



ptr   dat  40,  -20

for 8
      dat 0, 0
rof

start mov.i ptr, <ptr
      mov.i ptr, }ptr
      mov.i ptr, <ptr
      mov.i ptr, }ptr
      mov.i ptr, <ptr
      spl -5,    <(ptr-3)
      mov.i ptr, }ptr
      mov.i ptr, <ptr
      mov.i ptr, }ptr
      mov.i ptr, <ptr
      mov.i ptr, }ptr

end start
